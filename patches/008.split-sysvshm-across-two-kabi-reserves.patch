diff --git a/include/linux/sched.h b/include/linux/sched.h
--- a/include/linux/sched.h
+++ b/include/linux/sched.h
@@ -1652,13 +1652,8 @@ struct task_struct {
 	 * bytes on arm64, so slots 1-3 are an exact fit.
 	 */
 	ANDROID_KABI_USE(1, struct sysv_sem sysvsem);
-	_ANDROID_KABI_REPLACE(
-		ANDROID_KABI_RESERVE(2);
-		ANDROID_KABI_RESERVE(3),
-		struct {
-			struct sysv_shm sysvshm;
-		}
-	);
+	ANDROID_KABI_USE(2, struct list_head *sysvshm_next);
+	ANDROID_KABI_USE(3, struct list_head *sysvshm_prev);
 #else
 	ANDROID_KABI_RESERVE(1);
 	ANDROID_KABI_RESERVE(2);
@@ -1703,6 +1698,14 @@ struct task_struct {
 };
 
+#ifdef CONFIG_SYSVIPC
+static_assert(offsetof(struct task_struct, sysvshm_prev) ==
+	      offsetof(struct task_struct, sysvshm_next) +
+	      sizeof_field(struct task_struct, sysvshm_next));
+static_assert(sizeof_field(struct task_struct, sysvshm_next) +
+	      sizeof_field(struct task_struct, sysvshm_prev) ==
+	      sizeof(struct list_head));
+#endif
 #ifdef CONFIG_SCHED_PROXY_EXEC
 DECLARE_STATIC_KEY_FALSE(__sched_proxy_exec);
 static inline bool sched_proxy_exec(void)
 {
diff --git a/include/linux/shm.h b/include/linux/shm.h
--- a/include/linux/shm.h
+++ b/include/linux/shm.h
@@ -17,6 +17,8 @@ long do_shmat(int shmid, char __user *shmaddr, int shmflg, unsigned long *addr,
 	      unsigned long shmlba);
 void exit_shm(struct task_struct *task);
-#define shm_init_task(task) INIT_LIST_HEAD(&(task)->sysvshm.shm_clist)
+#define task_sysvshm_clist(task) \
+	((struct list_head *)&(task)->sysvshm_next)
+#define shm_init_task(task) INIT_LIST_HEAD(task_sysvshm_clist(task))
 #else
 struct sysv_shm {
 	/* empty */
diff --git a/ipc/shm.c b/ipc/shm.c
--- a/ipc/shm.c
+++ b/ipc/shm.c
@@ -446,11 +446,11 @@ void exit_shm(struct task_struct *task)
 		task_lock(task);
 
-		if (list_empty(&task->sysvshm.shm_clist)) {
+		if (list_empty(task_sysvshm_clist(task))) {
 			task_unlock(task);
 			break;
 		}
 
-		shp = list_first_entry(&task->sysvshm.shm_clist, struct shmid_kernel,
+		shp = list_first_entry(task_sysvshm_clist(task), struct shmid_kernel,
 				shm_clist);
 
 		/*
@@ -781,5 +781,5 @@ static int newseg(struct ipc_namespace *ns, struct ipc_params *params)
 	task_lock(current);
-	list_add(&shp->shm_clist, &current->sysvshm.shm_clist);
+	list_add(&shp->shm_clist, task_sysvshm_clist(current));
 	task_unlock(current);
 
 	/*
